APP_ENV=dev
APP_LOG_LEVEL=info

# API key for Flutter app (x-api-key header)
BRIDGE_API_KEY=change-me

# Upstream swap service (toncoin-cash-swap) for sub-legs
SWAP_API_BASE=http://127.0.0.1:8085
SWAP_API_KEY=change-me

CORS_ALLOW_ORIGINS=*
